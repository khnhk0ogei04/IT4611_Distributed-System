# JMS Sender Receiver Example

### Run application

##### Run sender
```shell script
$ ./gradlew runSender
```

##### Run receiver
```shell script
$ ./gradlew runReceiver
```

When both sender and receiver is running, start typing to console of sender and see output message at receiver